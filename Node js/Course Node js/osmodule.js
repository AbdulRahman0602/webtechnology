const os = require('os'); // no need to add the './' because os is a built in package.

console.log(os.freemem());//used to find the amount of free memory
console.log(os.homedir());//used to find the home directory
console.log(os.hostname());//used to find the host name


